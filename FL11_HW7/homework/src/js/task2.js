const game = confirm('Do you want to play a game?');

    if ( game === false) {
        alert('You did not become a billionaire, but can.');
    } else {
        prompt('Enter a number of pocket on which the ball could land.', '').Math.floor(Math.random() * 9);
    }
    
console.log(game);
